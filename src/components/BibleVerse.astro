---
const response = await fetch("https://beta.ourmanna.com/api/v1/get?format=json");
const data = await response.json()
const { text, reference } = data.verse.details
---

<div class="flex flex-col md:flex-row md:justify-between w-full items-center bg-primary py-4 md:py-6 px-2 md:px-4 lg:py-20 lg:px-24">
    <div class="text-center md:text-left">
        <h5>REFLECTION</h5>
        <h2 class="font-bold text-2xl md:text-3xl lg:text-4xl text-white">
            God’s Word Today
        </h2>
    </div>
    <div class="flex flex-col md:flex-row gap-[5%] items-center max-w-full md:max-w-lg lg:max-w-2xl">
        <div class="p-6 my-5 md:my-0 rounded-full bg-white flex items-center justify-center">
            <img src="/book-outline.svg" class="w-7 h-7"/>
        </div>
        <div class="w-full md:w-5/6 text-center md:text-left">
            <span class="bg-white py-1 px-3 rounded-3xl font-medium w-auto">
                {new Date().toDateString()}
            </span>
            <p class="text-white font-normal my-2 md:my-3 lg:my-5 text-xl md:text-2xl lg:text-3xl">
                {text}
            </p>
            <p class="text-white font-semibold text-md md:text-lg lg:text-xl">
                {reference}
            </p>
        </div>
    </div>
</div>